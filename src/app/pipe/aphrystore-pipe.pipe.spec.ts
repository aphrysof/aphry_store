import { AphrystorePipePipe } from './aphrystore-pipe.pipe';

describe('AphrystorePipePipe', () => {
  it('create an instance', () => {
    const pipe = new AphrystorePipePipe();
    expect(pipe).toBeTruthy();
  });
});
